# Mac Controller Configuration

# The PIN required for client devices to authenticate and connect
pin: "9563"

# Server configuration
server:
  port: 3000
  host: "0.0.0.0"
  ssl:
    enabled: true
    keyPath: "certs/key.pem"
    certPath: "certs/cert.pem"

# JWT secrets
jwt:
  accessTokenSecret: "access-token-secret-key-123"
  refreshTokenSecret: "refresh-token-secret-key-456"
